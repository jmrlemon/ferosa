<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Local notifications for order, appointment and message changes, raised
         by SummaryPollWorker. Runtime-requested from API 33. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- ARCore: optional so the app can install on non-AR devices -->
    <uses-feature android:name="android.hardware.camera.ar" android:required="false" />
    <uses-feature android:glEsVersion="0x00030000" android:required="false" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.Ferosa_landscaping"
        android:usesCleartextTraffic="${usesCleartextTraffic}">

        <meta-data android:name="com.google.ar.core" android:value="optional" tools:replace="android:value" />

        <!-- Hands a camera app a destination for photos taken to answer an
             <input type="file"> in the WebView. See res/xml/file_paths.xml. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Main launcher activity -->
        <!-- The Starter theme draws the splash; installSplashScreen() swaps in
             Theme.Ferosa_landscaping (its postSplashScreenTheme) on first frame. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Ferosa_landscaping.Starter"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AR Activity with deep link support.
             The portrait lock is deliberate: ARCore session tracking and the
             placement reticle are calibrated for portrait, and letting the
             activity recreate on rotation drops every placed model. Lint flags
             this generically, so it is suppressed here rather than repo-wide. -->
        <activity
            android:name=".ArActivity"
            android:exported="true"
            android:label="Ferosa AR"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Ferosa_landscaping"
            tools:ignore="LockedOrientationActivity,DiscouragedApi">

            <!-- Custom scheme deep link: ferosa://ar -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ferosa" />
                <data android:host="ar" />
            </intent-filter>

            <!-- HTTP/HTTPS fallback deep link -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:scheme="http" />
                <data android:host="ferosa.app" />
                <data android:pathPrefix="/ar" />
            </intent-filter>
        </activity>
    </application>

</manifest>
